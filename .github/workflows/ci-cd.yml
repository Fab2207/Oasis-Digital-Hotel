name: Java CI with Maven & Docker

on:
  push:
    branches: ["main"]
  pull_request:
    branches: ["main"]

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: "17"
          distribution: "temurin"
          cache: "maven"

      - name: Build with Maven
        run: mvn -B package --file pom.xml

  docker-push:
    needs: build-and-test
    runs-on: ubuntu-latest
    # Se ejecuta solo en la rama main
    if: github.ref == 'refs/heads/main'

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to Docker Hub
        # NOTA: Si VS Code muestra advertencias en 'secrets', ES NORMAL.
        # Las variables se leen desde GitHub Actions en la nube, no desde tu PC.
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build and push Docker image
        uses: docker/build-push-action@v5
        with:
          context: .
          push: true
          # Usamos tags múltiples para versionar la imagen
          tags: |
            ${{ secrets.DOCKERHUB_USERNAME }}/sistema-hotelero:latest
            ${{ secrets.DOCKERHUB_USERNAME }}/sistema-hotelero:${{ github.sha }}
